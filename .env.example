# Server Configuration
PORT=8000

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.vercel.app

# Rate Limiting Configuration (for learning/experimental use)
RATE_LIMIT_REQUESTS=5
RATE_LIMIT_WINDOW=60

# Redis Configuration (optional - for production rate limiting)
REDIS_URL=redis://localhost:6379

# Railway Configuration
RAILWAY_ENVIRONMENT=production

# Note: This is configured for educational/experimental use
# No authentication required for learning purposes
