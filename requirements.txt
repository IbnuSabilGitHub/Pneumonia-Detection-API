# FastAPI Backend (Lightweight)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# ML Inference (CPU-Only for Railway)
onnxruntime>=1.16.0
numpy>=1.24.0
pillow>=10.0.0

# Data Validation & Serialization
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# Security & Rate Limiting
slowapi>=0.1.9
PyJWT[crypto]>=2.8.0
requests>=2.28.0  # For fetching Supabase JWKS (ES256 token support)
httpx>=0.24.0  # For async Supabase REST API calls (user rate limiting)

# Logging & Monitoring
python-json-logger>=2.0.7

# Environment management
python-dotenv>=1.0.0